---
export interface Props {
  icon: string;
}

const { icon } = Astro.props;
const svg = await import(`../svg/${icon}.svg?raw`);
const innerHTML: string = svg.default;
---

<Fragment set:html={innerHTML} />
